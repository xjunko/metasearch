# Set secrets via wrangler:
#   wrangler secret put JWT_SECRET
#
# JWT_SECRET is required - used to sign/verify JWTs for search tokens
#
# The POST /api JSON endpoint is gated by a D1 database (binding DB), not env.
# See wrangler.toml and migrations/. No DB bound = API disabled.
